# Orthos — Python dependencies
#
# Normal users: just run  python main.py
#   All packages are installed automatically on first launch.
#
# Developers / manual setup:
#   pip install -r requirements.txt
#   Or install as package: pip install -e ".[dev]"
#   Platform-specific packages install only on the matching OS.

# ── Core UI & system ──────────────────────────────────────────────────────
PyQt6
PyQt6-WebEngine     # Rich markdown/LaTeX rendering via QWebEngineView
markdown-it-py      # Native Markdown chat renderer (no browser required)
matplotlib          # Native MathText equation rendering (no browser required)
psutil
pillow
sounddevice
numpy
soundfile
requests

# ── Web automation & search ───────────────────────────────────────────────
playwright
beautifulsoup4
duckduckgo-search

# ── OS control (cross-platform) ───────────────────────────────────────────
pyautogui
pyperclip
pygetwindow
send2trash
mss
opencv-python

# ── OS control (Windows only) ─────────────────────────────────────────────
pywinauto ; sys_platform == "win32"
comtypes  ; sys_platform == "win32"
uiautomation ; sys_platform == "win32"
pycaw     ; sys_platform == "win32"
win10toast; sys_platform == "win32"

# ── Media & file processing ───────────────────────────────────────────────
youtube-transcript-api
python-pptx
miniaudio
PyMuPDF              # Render scanned PDF pages for direct vision-model reading

# ── STT — choose one (auto-installed by main.py based on your config) ─────
faster-whisper        # Whisper  (default, recommended)
# vosk               # Vosk     (alternative — uncomment if preferred)

# ── TTS — choose one (auto-installed by main.py based on your config) ─────
edge-tts              # EdgeTTS  (default, free, requires internet)
# kokoro             # Kokoro   (fully offline — uncomment if preferred)
# elevenlabs         # ElevenLabs (cloud API — uncomment if preferred)

# ── Vector search (ChromaDB + sentence-transformers) ──────────────────────
chromadb
sentence-transformers

# ── Memory service (standalone HTTP API) ─────────────────────────────────
fastapi
uvicorn
pydantic
msgpack

# ── Telegram bot integration ────────────────────────────────────────────
python-telegram-bot

# ── Logging, observability & security ────────────────────────────────────
loguru
prometheus-client
# opentelemetry-api       # Uncomment for OpenTelemetry tracing
# opentelemetry-sdk
# opentelemetry-instrumentation-requests

# ── MCP Client ───────────────────────────────────────────────────────────
mcp>=1.27,<2

# ── Testing ──────────────────────────────────────────────────────────────
pytest
pytest-cov

# ── Gemini Live API (WebSocket-based) ────────────────────────────────────
google-genai

# ── Local LLM ─────────────────────────────────────────────────────────────
# Ollama must be installed separately: https://ollama.com
# Then pull a model:  ollama pull qwen2.5:7b
